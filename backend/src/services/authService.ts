export async function signup(email: string, password: string) {
  return { message: `Signup placeholder for ${email}` };
}

export async function login(email: string, password: string) {
  return { message: `Login placeholder for ${email}` };
}